Install PyInstaller:
pip install pyinstaller

Naviagte to the directory of the script
Run pyinstaller:
pyinstaller your_script_name.py

If you want a single file, you can use:
pyinstaller --onefile your_script_name.py

To create an executable without a console window, use:
pyinstaller --onefile --noconsole your_script_name.py

Alternativly, you can use auto-py-to-exe that provides a graphical interface for PyInstaller, simplifing the process: -- make sure you include the dice folder and images to files and the folder to makr it work correctly
pip install auto-py-to-exe
auto-py-to-exe



You can load "exe generation/auto_py_settings.json" to get the last build settings for the project when using auto-py-to-exe.

Dont forget to increase the version number. A script has been generated that will update the version number and open auto-py-to-exe in a single command.
I have called it: auto-py-to-exe-wrapper.py. It can be executed by:
python auto-py-to-exe-wrapper.py